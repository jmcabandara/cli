#!/bin/bash

set -e

echo -e "\nGenerating Binary..."

ROOT_DIR=$(cd $(dirname $(dirname $0)) && pwd)

$ROOT_DIR/bin/generate-language-resources

GO15VENDOREXPERIMENT=1 go build -o $ROOT_DIR/out/cf ./main
